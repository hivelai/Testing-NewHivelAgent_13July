// Fixture file used to demonstrate the rework/maintenance classification
// from metricsCalculator.classifyRemovedLines against real git blame history.
const a = 1;
const oldLine = 'removed after aging past REWORK_DAYS -> maintenance';
const b = 2;
